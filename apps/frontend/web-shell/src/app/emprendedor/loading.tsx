import { ViewLoader } from "@leanstart/commons";

/** Frontera de carga del módulo del emprendedor (empresas, canvas, hipótesis, productos…). */
export default function Loading() {
  return <ViewLoader message="Cargando módulo" />;
}
