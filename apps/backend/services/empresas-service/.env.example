PORT=4002
DATABASE_URL=postgresql://leanstart:leanstart@localhost:5432/leanstart_empresas
INTERNAL_KEY=dev-internal-key-change-me
NOTIFICACIONES_SERVICE_URL=http://localhost:4004

# auth-service — para registrar acciones administrativas en la bitácora de auditoría.
AUTH_SERVICE_URL=http://localhost:4001

# API key de Google Gemini (aistudio.google.com/apikey, tier gratuito, sin
# tarjeta) para el asistente conversacional de Alexa. Sin ella, el endpoint
# responde con un mensaje explicando que el asistente no está configurado
# (no revienta).
GEMINI_API_KEY=
