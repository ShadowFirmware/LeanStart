PORT=4002
DATABASE_URL=postgresql://leanstart:leanstart@localhost:5432/leanstart_empresas
INTERNAL_KEY=dev-internal-key-change-me
NOTIFICACIONES_SERVICE_URL=http://localhost:4004

# API key de Anthropic (console.anthropic.com) para el asistente conversacional
# de Alexa. Sin ella, el endpoint responde con un mensaje explicando que el
# asistente no está configurado (no revienta).
ANTHROPIC_API_KEY=
